import React from 'react'

import './topbar.scss'

const Topbar = () => {
    return (
        <header className="wn_topbar">
            <img src="./images/logo.svg"/>
        </header>
    )
}

export default Topbar